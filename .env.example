# 环境变量清单（提交到仓库，作为「有哪些变量」的唯一说明）
#
# 用法：
#   1. 三个环境的真实取值分别写在 .env.development / .env.test / .env.production；
#   2. 个人本地覆盖写在 .env.local（已被 .gitignore 忽略，不提交）；
#   3. 新增变量时，先在这里登记一份带说明的占位，再到对应 .env.* 填值。
#
# 暴露约定：
#   - 仅 PUBLIC_ 前缀的变量会被构建工具注入到客户端代码；
#   - 其余变量仅构建期（Node 侧）可见，不进打包产物；
#   - 密钥 / token 等敏感信息绝不加 PUBLIC_ 前缀，也不提交真实值。
#
# 注意：运行模式（dev / prod）无需手动维护 NODE_ENV，Vite 与 Rsbuild 会按
# 命令（dev / build）原生注入，isDev() / isProd() 直接消费构建工具的原生口径。

# 应用环境：development | test | production（业务逻辑分支开关）
APP_ENV=development

# 接口基地址（示例变量，演示「同名变量按环境切值」）
PUBLIC_API_BASE_URL=http://localhost:3000/api
