# 生产环境变量（正式上线）
#
# 约定：只有 PUBLIC_ 前缀的变量会被暴露到客户端，其余仅构建期可见。
# 真实密钥 / 私密配置绝不写进任何 PUBLIC_ 变量，也不提交到仓库。
#
# 运行模式（生产级构建优化）由 `pnpm build` 触发，构建工具原生注入，无需手动维护 NODE_ENV。

# 应用环境：业务侧识别为生产环境
APP_ENV=production

# 接口基地址（示例占位，按需替换为真实生产地址）
PUBLIC_API_BASE_URL=https://api.example.com/api
