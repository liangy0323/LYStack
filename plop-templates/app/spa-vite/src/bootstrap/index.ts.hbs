/**
 * 组合根（composition root）。
 *
 * 应用启动时的统一装配点：在这里集中完成所有「副作用初始化」与
 * 依赖注入（如认证 token 读取、服务层配置、第三方 SDK 初始化）。
 *
 * 设计意图：让启动流程显式、可读、可测；业务组件不再各自做初始化，
 * 一切装配收敛于此。当前底座阶段为空骨架，随 services / auth 层接入而填充。
 */
export function bootstrap(): void {
  // TODO(services): configureServiceAuth({ getToken, onAuthFailure })
  // TODO(auth): resolveTokenFromUrl()
}
